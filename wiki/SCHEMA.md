# Wiki Schema & Rules

1. **Structure**: Every entity must have its own page in `entities/`.
2. **Linking**: Use bidirectional links `[[PageName]]`.
3. **Consistency**: Check for contradictions when adding new sources.
4. **Maintenance**: LLM (Ollama) updates index and log on every ingest.
