{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Pages }}
  {{- /* 택소노미(tags/categories) 목록·term 페이지는 사이트맵에서 제외 — thin content, 크롤 예산 보호 */ -}}
  {{- if and .Permalink (not (in (slice "taxonomy" "term") .Kind)) (ne .Params.robotsNoIndex true) }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" | safeHTML }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>
    {{- end }}
    {{- /* hreflang은 색인 대상 번역본만 가리킨다.
           noindex 처리한 언어(ja/vi/id)를 대체 URL로 제시하면 국제화 신호가 모순된다. */ -}}
    {{- if .IsTranslated }}
    {{- range .AllTranslations }}
    {{- if ne .Params.robotsNoIndex true }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}" />
    {{- end }}
    {{- end }}
    {{- end }}
  </url>
  {{- end }}
  {{- end }}
</urlset>
